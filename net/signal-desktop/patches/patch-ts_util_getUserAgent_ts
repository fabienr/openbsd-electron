Index: ts/util/getUserAgent.ts
--- ts/util/getUserAgent.ts.orig
+++ ts/util/getUserAgent.ts
@@ -9,6 +9,8 @@ const PLATFORM_STRINGS: { [platform: string]: string }
   win32: 'Windows',
   darwin: 'macOS',
   linux: 'Linux',
+  freebsd: 'FreeBSD',
+  openbsd: 'OpenBSD',
 };
 
 export function getUserAgent(
