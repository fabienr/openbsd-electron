Index: scripts/afterpack.js
--- scripts/afterpack.js.orig
+++ scripts/afterpack.js
@@ -18,7 +18,9 @@ function getAppFileName(context) {
 	}
 }
 
+
 exports.default = async function afterPack(context) {
+/*
 	try {
 		const path = `${context.appOutDir}/${getAppFileName(context)}`;
 		await chmod(path, 0o755);
@@ -33,4 +35,5 @@ exports.default = async function afterPack(context) {
 		console.error('afterPack error: ', error);
 		process.exit(1);
 	}
+*/
 };
