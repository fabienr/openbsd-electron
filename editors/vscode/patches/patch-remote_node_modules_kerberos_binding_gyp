Index: remote/node_modules/kerberos/binding.gyp
--- remote/node_modules/kerberos/binding.gyp.orig
+++ remote/node_modules/kerberos/binding.gyp
@@ -54,11 +54,34 @@
             ]
           }
         }],
-        ['OS=="mac" or OS=="linux"', {
+        ['OS=="freebsd" or OS=="linux" or OS=="mac" or OS=="openbsd"', {
           'sources': [
             'src/unix/base64.cc',
             'src/unix/kerberos_gss.cc',
             'src/unix/kerberos_unix.cc'
+          ]
+        }],
+        ['OS=="openbsd"', {
+          'include_dirs': [
+            '/usr/local/heimdal/include',
+          ],
+          'link_settings': {
+            'libraries': [
+              '-lkrb5',
+              '-lgssapi'
+            ],
+            'library_dirs': [
+              '/usr/local/heimdal/lib',
+            ],
+          },
+          'conditions': [
+            ['_type=="static_library"', {
+              'link_settings': {
+                'libraries': [
+                  '-lcom_err'
+                ]
+              }
+            }]
           ]
         }],
         ['(OS=="mac" or OS=="linux") and (kerberos_use_rtld!="true")', {
